<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.Bibibi.mappers.UserVideoSeriesVideoMappers">
	<!--实体映射-->
	<resultMap id="base_result_map" type="com.Bibibi.entity.po.UserVideoSeriesVideo">
		<!-- 列表ID -->
		<result column="series_id" property="seriesId"/>
		<!-- 视频ID -->
		<result column="video_id" property="videoId"/>
		<!-- 用户ID -->
		<result column="user_id" property="userId"/>
		<!-- 排序 -->
		<result column="sort" property="sort"/>

	</resultMap>

	<!--通用查询列-->
	<sql id="base_column_list">
		series_id,video_id,user_id,sort
	</sql>

	<!--基础查询条件-->
	<sql id="base_query_condition">

		<if test="query.seriesId!=null">
			and series_id = #{query.seriesId}
		</if>
		<if test="query.videoId!=null and query.videoId!=''">
			and video_id = #{query.videoId}
		</if>
		<if test="query.userId!=null and query.userId!=''">
			and user_id = #{query.userId}
		</if>
		<if test="query.sort!=null">
			and sort = #{query.sort}
		</if>
	</sql>

	<!--拓展的查询条件-->
	<sql id="base_query_condition_extend">
		<if test="query.videoIdFuzzy!=null and query.videoIdFuzzy!=''">
			and video_id like concat ('%', #{query.videoIdFuzzy}, '%')
		</if>
		<if test="query.userIdFuzzy!=null and query.userIdFuzzy!=''">
			and user_id like concat ('%', #{query.userIdFuzzy}, '%')
		</if>

	</sql>

	<!--通用的查询条件-->
	<sql id="query_condition">
		<where>
			<include refid="base_query_condition"/>
			<include refid="base_query_condition_extend"/>
		</where>
	</sql>

	<!--查询列表-->
	<select id = "selectList" resultMap="base_result_map">
		SELECT
		<include refid="base_column_list"/>
		FROM user_video_series_video
		<include refid="query_condition"/>
		<if test="query.orderBy!=null">order by ${query.orderBy}</if>
		<if test="query.simplePage!=null">limit #{query.simplePage.start},#{query.simplePage.end}</if>
	</select>

	<!--查询数量-->
	<select id = "selectCount" resultType="java.lang.Integer">
		SELECT COUNT(1) FROM user_video_series_video
		<include refid="query_condition"/>
	</select>

	<!--插入 （匹配有值的字段）-->
	<insert id = "insert" parameterType="com.Bibibi.entity.po.UserVideoSeriesVideo">

		INSERT INTO user_video_series_video
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="bean.seriesId != null">
				series_id,
			</if>
			<if test="bean.videoId != null">
				video_id,
			</if>
			<if test="bean.userId != null">
				user_id,
			</if>
			<if test="bean.sort != null">
				sort,
			</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="bean.seriesId != null">
				#{bean.seriesId},
			</if>
			<if test="bean.videoId != null">
				#{bean.videoId},
			</if>
			<if test="bean.userId != null">
				#{bean.userId},
			</if>
			<if test="bean.sort != null">
				#{bean.sort},
			</if>
		</trim>
	</insert>

		<!-- 插入或者更新 （匹配有值的字段） -->
	<insert id = "insertOrUpdate" parameterType="com.Bibibi.entity.po.UserVideoSeriesVideo">
		INSERT INTO user_video_series_video
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="bean.seriesId != null">
				series_id,
			</if>
			<if test="bean.videoId != null">
				video_id,
			</if>
			<if test="bean.userId != null">
				user_id,
			</if>
			<if test="bean.sort != null">
				sort,
			</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="bean.seriesId != null">
				#{bean.seriesId},
			</if>
			<if test="bean.videoId != null">
				#{bean.videoId},
			</if>
			<if test="bean.userId != null">
				#{bean.userId},
			</if>
			<if test="bean.sort != null">
				#{bean.sort},
			</if>
		</trim>
		on DUPLICATE key update
		<trim prefix="" suffix="" suffixOverrides=",">
			<if test="bean.userId != null">
				 user_id = VALUES(user_id)
			</if>
			<if test="bean.sort != null">
				 sort = VALUES(sort)
			</if>
		</trim>
	</insert>

	<!-- 批量插入 -->
	<insert id ="insertBatch" parameterType="com.Bibibi.entity.po.UserVideoSeriesVideo">
		INSERT INTO user_video_series_video(series_id,video_id,user_id,sort) VALUES
		<foreach collection="list" item="item" separator=",">
			(#{item.seriesId},#{item.videoId},#{item.userId},#{item.sort})
		</foreach>
	</insert>

	<!-- 批量插入（或者更新） -->
	<insert id ="insertOrUpdateBatch" parameterType="com.Bibibi.entity.po.UserVideoSeriesVideo">
		INSERT INTO user_video_series_video(series_id,video_id,user_id,sort) VALUES
		<foreach collection="list" item="item" separator="," >
			(#{item.seriesId},#{item.videoId},#{item.userId},#{item.sort})
		</foreach>
			on DUPLICATE key update
			series_id= VALUES(series_id),video_id= VALUES(video_id),user_id= VALUES(user_id),sort= VALUES(sort)
	</insert>

	<!-- 根据SeriesIdAndVideoId查询 -->
	<select id="selectBySeriesIdAndVideoId" resultMap="base_result_map">
		select <include refid="base_column_list"/> from user_video_series_video where series_id=#{seriesId} and video_id=#{videoId}
	</select>

	<!-- 根据SeriesIdAndVideoId更新 -->
	<update id="updateBySeriesIdAndVideoId" parameterType="com.Bibibi.entity.po.UserVideoSeriesVideo">
		update user_video_series_video
		<set>
			<if test="bean.seriesId != null">
				series_id = #{bean.seriesId},
			</if>
			<if test="bean.videoId != null">
				video_id = #{bean.videoId},
			</if>
			<if test="bean.userId != null">
				user_id = #{bean.userId},
			</if>
			<if test="bean.sort != null">
				sort = #{bean.sort},
			</if>
		</set>
		where series_id=#{seriesId} and video_id=#{videoId}
	</update>

	<!-- 根据SeriesIdAndVideoId删除 -->
	<delete id="deleteBySeriesIdAndVideoId">
		delete from user_video_series_video where series_id=#{seriesId} and video_id=#{videoId}
	</delete>
</mapper>